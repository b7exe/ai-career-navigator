# AI Career Navigator 🚀

### Team Roles
- **Person A (Logic Lead):** Responsible for `logic/engine.py` (Gemini API integration).
- **Person B (UI Lead - b7):** Responsible for `app.py`, `templates/`, and `static/`.

### How to Contribute
1. **Pull** the latest changes: `git pull origin main`
2. **Work** in your designated folder.
3. **Push** your changes: 
   - `git add .`
   - `git commit -m "Added [feature name]"`
   - `git push origin main`
